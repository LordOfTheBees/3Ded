



1) Изменить логику CoordinateSytem. Хранить только одну систему координат, а в generateTransMatrix отдавать базовую систему координат. 
	1.1) Соответственно изменить TDRenderWindow. Хранить в ней две матрицы: camera_coord_system, world_coord_system. 
	1.2) Так же изменить Model. Хранить только own_coord_system,а camera_coord_system передавать в методе для генерации матрицы.

2) Сделать CoordinateSystem базовым для классов Model, Camera

3) Создать новый класс World : public CoordinateSystem



Исправление ошибок